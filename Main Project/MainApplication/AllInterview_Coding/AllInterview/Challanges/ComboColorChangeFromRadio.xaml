<ui:WslDialog  xmlns:ui="clr-namespace:Keysight.Ccl.Wsl.UI;assembly=Keysight.Ccl.Wsl"
               x:Class="MainApplication.WPF_Assignment.Common.ComboColorChangeFromRadio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApplication.WPF_Assignment.Common"
        mc:Ignorable="d"
        Title="ComboColorChangeFromRadio" Height="450" Width="800" Left="8" Top="62">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <RadioButton Height="20"  GroupName="grp1" Content="Dark" x:Name="DarkRadio" IsChecked="True"></RadioButton>
            <RadioButton Height="20" GroupName="grp1" Content="Light" x:Name="LightRadio"  ></RadioButton>
        </StackPanel>
        <ComboBox Grid.Row="2" x:Name="cmbColor">
            <ComboBoxItem Content="Item1"></ComboBoxItem>
            <ComboBoxItem Content="Item2"></ComboBoxItem>
            <ComboBoxItem Content="Item3"></ComboBoxItem>
            <ComboBoxItem Content="Item4"></ComboBoxItem>
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsChecked,ElementName=LightRadio}" Value="True">
                            <Setter Property="Foreground" Value="Black" />
                            <Setter Property="Background" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsChecked,ElementName=DarkRadio}" Value="True">
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="Background" Value="Black"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>
    </Grid>
</ui:WslDialog>
