<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MVVMCore.WPF_MVVM">
                    <Style TargetType="{x:Type local:CustomLabelControl}">
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomLabelControl}">
                    <Border Background="LightGray" Padding="10">
                        <TextBlock Text="{Binding LabelText, RelativeSource={RelativeSource TemplatedParent}}" 
                                   FontSize="16" 
                                   Foreground="Black" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
