<Window x:Class="MVVMCore.DataGridSingleClickCheckbox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMCore.DataGridSingleClickCheckbox.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Left="8" Top="62">
    <Grid>

        <DataGrid ItemsSource="{Binding MyCollection}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">

            <DataGrid.Columns>

                <!--<DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" />-->

                <DataGridTemplateColumn Header="Select">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="FirstName" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="LastName" Binding="{Binding LastName}"/>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
