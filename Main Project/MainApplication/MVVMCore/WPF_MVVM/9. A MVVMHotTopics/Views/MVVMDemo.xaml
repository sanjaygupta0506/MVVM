<ui:WslDialog  xmlns:ui="clr-namespace:Keysight.Ccl.Wsl.UI;assembly=Keysight.Ccl.Wsl" x:Class="MainApplication.WPF_MVVM._9._A_MVVMHotTopics.Views.MVVMDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApplication.WPF_MVVM._9._A_MVVMHotTopics.Views"
        mc:Ignorable="d"
        Title="MVVMDemo" Height="450" Width="800" Left="8" Top="62">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtName" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="0" Grid.Column="0" Width="100" Height="30" HorizontalAlignment="Left" ></TextBox>
        <Button Name="btnSubmit" Content="Submit" Command="{Binding SubmitCommand}" Grid.Row="1" Grid.Column="0" Width="100" Height="30" HorizontalAlignment="Left"></Button>
        <ListBox ItemsSource="{Binding MyItems}" Grid.Row="2" Grid.Column="0" Width="100" HorizontalAlignment="Left" Height="80"></ListBox>
    </Grid>
</ui:WslDialog>
