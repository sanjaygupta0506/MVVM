<ui:WslDialog  xmlns:ui="clr-namespace:Keysight.Ccl.Wsl.UI;assembly=Keysight.Ccl.Wsl" x:Class="MVVMCore.Templates.DataTemplates.Templete_Data"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMCore.Templates.DataTemplates"
        mc:Ignorable="d"
        Title="Data Template Example" Height="450" Width="800" Left="8" Top="62">
    <ui:WslDialog.Resources>
        <DataTemplate x:Key="x_MyTemplate">
            <Label Content="{Binding Name}" Foreground="Yellow" FontWeight="Bold"/>
        </DataTemplate>
    </ui:WslDialog.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--Ist Way-->
        <Label Content="{Binding person}" Grid.Row="0">
            <Label.ContentTemplate>
                <DataTemplate>
                    <Label Content="{Binding Name}" Foreground="Red" FontWeight="Bold"/>
                </DataTemplate>
            </Label.ContentTemplate>
        </Label>

        <!--2rd Way-->
        <ContentPresenter Content="{Binding person}" Grid.Row="1">
            <ContentPresenter.ContentTemplate>
                <DataTemplate>
                    <Label Content="{Binding Name}" Foreground="Blue" FontWeight="Bold"/>
                </DataTemplate>
            </ContentPresenter.ContentTemplate>
        </ContentPresenter>

        <!--3th Way-->
        <ContentPresenter Content="{Binding person}" ContentTemplate="{StaticResource x_MyTemplate}" Grid.Row="2">
        </ContentPresenter>
        <!--4th Way-->
        <ListBox Name="dataTemplate" Grid.Row="3">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="{Binding CoverImage}" Height="20" Width="20" Margin="0,0,20,0"></Image>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="16"></TextBlock>
                            <TextBlock Text="{Binding Author}" FontSize="16"></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</ui:WslDialog>
