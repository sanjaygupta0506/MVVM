<ui:WslDialog  xmlns:ui="clr-namespace:Keysight.Ccl.Wsl.UI;assembly=Keysight.Ccl.Wsl"
               x:Class="MainApplication.WPF_Assignment.Common.BtnEnableOnTextChange"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApplication.WPF_Assignment.Common"
        mc:Ignorable="d"
        Title="BtnEnableOnTextChange" Height="450" Width="800" Left="8" Top="62">
    <Window.Resources>
        <!--<Style x:Key="DeleteButton">

            <Setter Property="IsEnabled" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=SelectedPath}" Value="{x:Null}">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=SelectedPath.Length}" Value="0">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>

        </Style>-->
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="Text" Margin="0,0,10,0"></TextBlock>
            <TextBox Margin="10,0,10,0"  Width="100" Text="{Binding TextData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></TextBox>
        </StackPanel>
        <Button x:Name="EnableButton" Grid.Row="3" IsEnabled="{Binding IsSubmitButtonEnabled}" Content="Submit">
            <!--Style="{StaticResource DeleteButton}"-->

        </Button>
    </Grid>
</ui:WslDialog>
